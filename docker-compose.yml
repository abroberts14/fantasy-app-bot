version: '3.8'

services:

  backend:
    build: ./services/backend
    ports:
      - 5000:5000
    environment:
      - DATABASE_URL=postgres://postgres:postgres1!@srcbot1.c7okouiqu9jh.us-east-1.rds.amazonaws.com:5432/srcbot1
      - SECRET_KEY=09d25e094faa6caaaron8166b7a9563b93f79f6f0f4caa6cf63b88e8d3e7

    volumes:
      - ./services/backend:/app
  
    command: >
      bash -c "aerich upgrade && uvicorn src.main:app --reload --host 0.0.0.0 --port 5000"

  frontend:
    build: ./services/frontend
    volumes:
      - './services/frontend:/app'
      - './services/frontend/src:/app/src'
      - '/app/node_modules'
    ports:
      - 5173:5173
    environment:
      - SECRET_KEY=09d25e094faa6caaaron8166b7a9563b93f79f6f0f4caa6cf63b88e8d3e7
